/**
 * Internal dependencies
 */
const {
	getEntries,
} = require('./file');

const {
	getArgFromCLI,
	hasArgInCLI,
} = require('./process');

module.exports = {
	getEntries,
	getArgFromCLI,
	hasArgInCLI,
}
